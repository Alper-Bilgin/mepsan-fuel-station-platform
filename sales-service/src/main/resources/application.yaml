server:
  port: 8083

spring:
  application:
    name: sales-service
  datasource:
    url: jdbc:oracle:thin:@localhost:1521/FREEPDB1
    username: mepsan_admin
    password: mepsan123
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration
  rabbitmq:
    host: localhost
    port: 5672
    username: mepsan_mq
    password: mepsan123
  redis:
    host: localhost
    port: 6379

management:
  endpoints:
    web:
      exposure:
        include: health,info